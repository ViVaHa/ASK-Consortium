1. Run this command on the terminal(Only once during first time in this location):
npm install

2. Run this command on the terminal(Only once during first time inside client folder):
npm install

3. Run this command on the terminal(Only once during first time inside backend folder):
npm install

To open the visualization enter the following commands in this folder on the terminal and wait for a minute:
npm start
